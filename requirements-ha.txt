# Current pytest harness for Home Assistant 2026.7.2 (requires Python >=3.14).
# Declared HACS minimum is homeassistant>=2026.3.0, which also requires Python 3.14.2+.
pytest-homeassistant-custom-component==0.13.346
