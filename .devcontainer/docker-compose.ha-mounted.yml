services:
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:2026.3.0
    container_name: ha-ebay-homeassistant-mounted
    volumes:
      - ./ha-config:/config
      - ../custom_components/ebay:/config/custom_components/ebay:ro
    ports:
      - "8123:8123"
    environment:
      TZ: ${TZ:-America/New_York}
    restart: unless-stopped
